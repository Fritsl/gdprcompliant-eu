---
id: S-02
title: Pass A collector
stream: S
phase: 2
est: M
status: done
owner: claude
claimed: 2026-09-03 18:42
completed: 2026-09-03 18:54
depends: S-01
verify: pnpm test:integration -- pass-a
---
# S-02 · Pass A collector

## Goal
Load, wait for network quiet, touch nothing. Capture every request, every cookie, localStorage and sessionStorage writes, and a full-page screenshot.

## Acceptance criteria
- [x] Requests captured with initiator chain, timing and resource type
- [x] Cookies captured with domain, expiry, secure, httpOnly and sameSite
- [x] Network-quiet heuristic documented and tested against a fixture that lazy-loads a tracker after 4 seconds
- [x] Evidence written content-addressed, referenced by hash from findings

## How this is verified
```bash
pnpm test:integration -- pass-a
```

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-03 18:42 — claimed by claude
- 2026-09-03 claude: collectPassA(pool, target) in packages/scanner/src/passes: load, then the network-quiet heuristic (docs/decisions/network-quiet.md: floor 5s, 1.5s silence, cap 15s, all recorded on the capture). Requests via Playwright events plus a CDP Network session for initiators (script URL and chain, or parser with the document line, which is Chromium's attribution for parser-blocking scripts); cookies from context.cookies(); storage writes via an init script patching setItem plus a final snapshot; full-page PNG. PassCaptureSchema in contracts. captureToEvidence gives content-addressed Evidence rows (id = kind:hash16); canonicalJson/sha256 moved to contracts and shared with the remedy lock. New fixture lazy-tracker (tag on a 4s timer) proves networkidle misses it and Pass A catches it; the F-07 fixture test now runs on Pass A. lazy-tracker expects CNS-01, which has no catalogue remedy yet: S-05 or R-03 must add one before R-02's completeness check covers detectors.
- 2026-09-03 18:54 — done — Pass A collector: requests with initiator chains and timing, cookies, storage writes, screenshot, documented network-quiet heuristic proven on a 4s lazy tracker, content-addressed evidence
