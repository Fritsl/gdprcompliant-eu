---
id: S-14
title: Finding assembly and severity
stream: S
phase: 2
est: M
status: done
owner: claude
claimed: 2026-09-04 01:07
completed: 2026-09-04 01:15
depends: S-05, R-01
verify: pnpm test:integration -- finding-assembly
---
# S-14 · Finding assembly and severity

## Goal
Assemble detector output into persisted findings with severity, evidence references, jurisdiction bindings and a resolved remedy.

## Acceptance criteria
- [x] Severity is computed from a documented rule table, not chosen ad hoc per detector
- [x] Assembly refuses to persist a finding lacking evidence or a remedy
- [x] Identical scans produce byte-identical finding sets — determinism is asserted in a test
- [x] Findings carry a stable id so the same problem is recognisable across re-scans

## How this is verified
```bash
pnpm test:integration -- finding-assembly
```

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-04 09:30 — Severity from one table: the type's base in detectors.json and four documented rules in severity.json (observed grade, three or more hosts, health/education sector by NACE prefix, regressed after closure), applied in order with the applied rules recorded on the decision; docs/decisions/severity.md. assembleFindings in @gc/findings takes every check's observations (security, forms, replay, policy discovery, certificate transparency, the consent differ's drafts) plus raw drafts, folds drafts of one identity, refuses a draft without evidence (DraftWithoutEvidence), an unregistered type or a type without a remedy (NoRemedy), and returns findings in fingerprint order with a digest; ids are findingId(case, fingerprint). reconcileFindings in @gc/db: seen again keeps the row and stamps lastSeenAt, gone is closed, back after closure is regressed and one level up. tests/integration/finding-assembly.test.ts assembles the insecure shop twice and asserts the same bytes, then stores, re-scans, fixes and regresses HSTS against the database.
- 2026-09-04 01:07 — claimed by claude
- 2026-09-04 01:15 — done — severity from a documented table, assembly that refuses missing evidence or remedy, byte-identical determinism, stable ids with re-scan reconciliation
