---
id: S-03
title: Consent banner detection and refusal-path automation
stream: S
phase: 2
est: L
status: done
owner: claude
claimed: 2026-09-04 00:29
completed: 2026-09-04 00:41
depends: S-02
verify: pnpm test:integration -- banner
---
# S-03 · Consent banner detection and refusal-path automation

## Goal
Find the banner, identify the platform where recognisable, and click through to a genuine refusal however many layers deep it is buried.

## Acceptance criteria
- [x] Handles the top consent platforms by signature, plus a generic heuristic fallback
- [x] Follows multi-layer refusal: settings, then toggles off, then save
- [x] Records the click path taken as evidence, including a screenshot per step
- [x] Reports honestly when it cannot find a refusal path rather than guessing — that itself is a finding
- [x] Passes against at least 8 banner fixtures covering different patterns

## How this is verified
```bash
pnpm test:integration -- banner
```

## Notes
The hardest task in phase 2 and the one the whole wedge rests on. Budget accordingly. When uncertain, prefer "could not determine" over a false pass.

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-04 07:30 — refuseConsent in @gc/scanner: banner found by platform signature (Cookiebot, OneTrust, Usercentrics, Cookie Information, Didomi, Quantcast, Klaro, CookieYes, TrustArc, Complianz) or by the heuristic (a pinned or dialog element that talks about cookies and offers controls), in the page or any frame, through open shadow roots; refusal by the direct "no" (Danish, English, German button text or the platform's selector), else settings → every enabled toggle off (checkboxes and aria switches, read by state) → save; a screenshot evidence row per step with the step pointing at it; "refused" only when the banner is gone afterwards; otherwise undetermined with the reason and the CNS-03 finding (new detector, remedy cns-03-offer-a-refusal in every jurisdiction, bindings in DK and DE). Eight banner fixtures: direct reject, two-layer checkboxes, Cookiebot-like, OneTrust-like, Usercentrics in a shadow root, banner in an iframe, accept-only, German aria switches; plus no-banner on the brochure. Fixture scripts live in files because the fixtures' CSP blocks inline script, and the framed page is a routed answer with its own frame-ancestors. Not done here: honouring the refusal (Pass B network diff, S-04) and closed shadow roots.
- 2026-09-04 00:29 — claimed by claude
- 2026-09-04 00:41 — done — banner by signature or heuristic, refusal by direct no or settings/toggles/save, screenshot per step, undetermined is CNS-03; eight banner fixtures
