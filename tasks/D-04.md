---
id: D-04
title: Job advert stack extraction
stream: D
phase: 4
est: M
status: done
owner: claude
claimed: 2026-09-04 10:29
completed: 2026-09-04 10:36
depends: D-03
verify: pnpm test:evals -- job-extraction
---
# D-04 · Job advert stack extraction

## Goal
Find current adverts and extract named tools into candidate processors.

## Acceptance criteria
- [x] Only public listings; nothing behind a login
- [x] Extraction is conservative — a tool is only claimed when named explicitly
- [x] Each extracted tool carries the advert URL and date as evidence

## How this is verified
```bash
pnpm test:evals -- job-extraction
```

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-04 14:45 — packages/scanner/src/adverts/index.ts with data/adverts/tools.json. The tool map is content: 26 tools a job advert names that process personal data, each with the names as adverts write them, `caseSensitive` where a name is also an ordinary word (Slack, Notion, Teams, Stripe, Dinero, SAP, AWS, Jira), a vendorId into the vendor registry where the operator is known and otherwise the country it contracts from. extractTools claims a tool only when one of its names stands on its own in the text (word boundaries over letters and digits, the case as declared), and quotes the words around the first mention; nothing is inferred from a skill or a title. advertCandidates skips every advert behind a login or not answering 200, by reason; writes one document evidence row per advert whose body carries the address, the posting date, the fetch time and the text, so the URL and date are under the hash; and makes one candidate Vendor per tool resting on every advert that names it, seen from the earliest posting date, resolved through the registry where a vendorId exists and unresolved with a country otherwise. discoverAdverts reads the site's own careers page from the home page's links (careers, jobs, ledige stillinger, karriere, offene stellen …), then the advert pages it links to, same site only, a bounded number of pages, marking a page that asks for a password rather than reading it; the page reader is now shared with the policy discovery (readPage). fixtures/adverts/set.json: twelve adverts in Danish, English and German with the tools each names, including a login-walled one, a gone one, and two written to tempt a careless extractor (CRM and ERP as skills; slack, notion and teams as ordinary words). tests/evals/job-extraction.test.ts: no false claim on any advert and at least 95% of the named tools found, the tempters yield nothing, mentions quote the text as written, the skipped adverts are skipped by reason, candidates carry address and date on hashed evidence, Microsoft 365 rests on three adverts from the earliest, Salesforce stays unresolved with US, the result is deterministic, and the careers link is found in every language and never off-site. The discovery is not yet wired into the scan worker; it runs as a planner task (crawl) once the dispatcher does.
- 2026-09-04 10:29 — claimed by claude
- 2026-09-04 10:36 — done — tools claimed only when named as written; every candidate rests on the advert with its address and date; login-walled and gone adverts skipped; measured over twelve adverts with no false claim
