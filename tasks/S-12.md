---
id: S-12
title: Security surface checks
stream: S
phase: 2
est: M
status: done
owner: claude
claimed: 2026-09-03 19:07
completed: 2026-09-03 19:23
depends: S-01
verify: pnpm test:integration -- security-surface
---
# S-12 · Security surface checks

## Goal
The Article 32 surface a stranger can see: TLS, HSTS, headers, mixed content, downgrade on form posts, referrer leakage, exposed paths.

## Acceptance criteria
- [x] Exposed-path probing is limited to a documented, conservative list and respects robots directives
- [x] Never attempts authentication, never sends a payload, never enumerates users beyond what a single public endpoint returns
- [x] Each check maps to a finding with a remedy that is a concrete config change

## How this is verified
```bash
pnpm test:integration -- security-surface
```

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-03 19:07 — claimed by claude
- 2026-09-03 claude: runSecurityChecks(pool, target, {capture, identity}) in packages/scanner/src/checks: transport (SEC-01), form downgrade (SEC-02, GET on an http action with no body to see the bounce), HSTS (SEC-03), mixed content (SEC-04), referrer leakage (SEC-05), security headers (SEC-06), exposed paths (SEC-07: twelve GET-only paths with a shape test each, robots.txt honoured; rules in docs/decisions/exposed-paths.md). Observations are SecurityObservationSchema (contracts) with evidence refs; a failed check without evidence is unrepresentable. Six new self_fix remedies with nginx snippets (sec-01, 03..07). Fixture server now terminates TLS on CONNECT with a self-signed cert (selfsigned), gives plain tunnels to CONNECTs on other ports, supports _headers.json and scheme-specific routes; pools take ignoreHTTPSErrors for the estate. New fixture insecure-forms fails all seven; clean-brochure passes all seven over TLS. Chromium auto-upgrades mixed images, so only scripts show as mixed.
- 2026-09-03 19:23 — done — Seven deterministic security checks with evidence and remedies; TLS-capable fixture estate; conservative documented path probe honouring robots
