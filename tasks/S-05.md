---
id: S-05
title: Three-pass differ
stream: S
phase: 2
est: M
status: done
owner: claude
claimed: 2026-09-04 00:53
completed: 2026-09-04 01:07
depends: S-04
verify: pnpm test:unit -- differ && pnpm test:integration -- consent-findings
---
# S-05 · Three-pass differ

## Goal
Diff the three captures into the consent findings — the wedge itself.

## Acceptance criteria
- [x] CNS-01 fires when non-essential requests occur before any interaction
- [x] CNS-02 fires when the reject-all set is materially the same as the accept-all set, reporting the exact overlapping hosts
- [x] CNS-03..08 cover missing reject on first layer, pre-ticked toggles, choice not persisted, and refusal path deeper than two layers
- [x] Every finding names the specific hosts and carries the diff as evidence
- [x] False-positive check: a correctly-behaving fixture produces zero consent findings

## How this is verified
```bash
pnpm test:unit -- differ && pnpm test:integration -- consent-findings
```

## Notes
The last acceptance line matters most. A scanner that cries wolf on a compliant site is worse than no scanner.

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-04 08:50 — diffPasses in @gc/scanner: every third-party host is judged by what it did (reports to collect/pixel paths, loads pixels with parameters, its script wrote identifiers, sets its own cookies), never by a list of names; the consent platform is the script present on every pass that tracks nothing on a page where a consent record was written. CNS-01 = tracking hosts on the first load; CNS-02 = tracking hosts contacted after the refusal exactly as after acceptance, naming the overlap; CNS-03 no refusal path; CNS-04 choice not remembered; CNS-05 no reject on the first layer (refusal went through settings and save); CNS-06 pre-ticked toggles (switched off during the refusal); CNS-07 refusal buried (more than three interactions). Detectors registered for CNS-01/02/05/06/07, remedies cns-05/06/07 in every jurisdiction, bindings in DK and DE. Every finding names its hosts and points at a pass_diff evidence row holding the whole diff; the path findings also point at the refusal screenshots. tests/unit/scanner/differ.test.ts on hand-built captures; tests/integration/consent-findings.test.ts over the fixtures, with each fixture's consent must/mustNot checked at the scanner's own dwell. The banner fixtures now honour a stored choice on reload, as a platform does; only banner-forgets forgets.
- 2026-09-04 00:53 — claimed by claude
- 2026-09-04 01:07 — done — three-pass differ: hosts judged by behaviour, CNS-01..07 with hosts named and the diff as evidence, zero findings on the fixtures that behave
