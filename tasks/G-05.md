---
id: G-05
title: Drift check: policy versus live site
stream: G
phase: 5
est: M
status: done
owner: claude
claimed: 2026-09-04 07:57
completed: 2026-09-04 08:26
depends: G-02, C-05
verify: pnpm test:integration -- policy-drift
---
# G-05 · Drift check: policy versus live site

## Goal
Flag when the published document no longer matches what the crawler sees.

## Acceptance criteria
- [x] A newly added vendor not named in the policy raises a specific finding within one watch cycle
- [x] Drift findings name both sides — what the policy says and what the site does
- [x] No false alarms on cosmetic changes, asserted against a fixture that changes markup but not behaviour

## How this is verified
```bash
pnpm test:integration -- policy-drift
```

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-04 14:30 — A new finding type, POL-05 (the site sends data to a company the published policy does not name; Art. 13(1)(e), 5(1)(a) in DK and DE; remedy pol-05-name-the-recipient, a generated_artefact that writes the policy again; guide pol-05 in en and da; lock at 36). packages/db/src/drift.ts: the published privacy policy's trace comments name the vendor rows it was written from; the watch's recipients read now names every third-party host the first load contacted, pass or fail, with an evidence row either way; compareRecipients resolves each host through the caller's HostResolver (the scanner's resolveHost, passed in so the database package needs no browser) and lists the hosts whose company the policy does not name. driftEvidence writes both sides as one evidence row (what the policy names, what the site contacts, as caption and as JSON); driftDrafts raises POL-05 on the read and on that row, subject the site. runWatch now runs the recipients family too and folds the drift drafts into the assembly, so a new vendor raises the finding within one watch cycle and the same site again is seen again, not raised again. Only hosts are compared, so markup that changes without a new host is not drift. seedRegister seeds every third-party recipient, resolved or kept as a host, not only the ones outside the EEA, and confirming a row confirms the vendor's transfer statement with it. The catalogue suite exempts a type raised from case data (detector db/…) from needing a fixture page; tests/integration/policy-drift.test.ts publishes a policy for tags.shop.test, then watches a cosmetic change (no finding) and a page that now loads Hotjar (one POL-05 naming Google Ireland Limited on the policy's side and static.hotjar.com (Hotjar Limited) on the site's, then seen again). The worker has no watch registration yet; when it gets one, it passes resolveHost as WatchOptions.resolve.
- 2026-09-04 07:57 — claimed by claude
- 2026-09-04 08:26 — done — POL-05 drift finding from the published policy's traced recipients vs every host the watch sees, both sides on its evidence; cosmetic change raises nothing
