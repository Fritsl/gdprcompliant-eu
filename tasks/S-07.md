---
id: S-07
title: Host to legal entity resolution
stream: S
phase: 2
est: L
status: done
owner: claude
claimed: 2026-09-04 06:54
completed: 2026-09-04 07:02
depends: S-02
verify: pnpm test:unit -- vendor-resolve
---
# S-07 · Host to legal entity resolution

## Goal
Resolve third-party hosts to named legal entities, their place of establishment and their ultimate corporate parent.

## Acceptance criteria
- [x] Curated vendor registry with provenance on every entry, versioned in the repo
- [x] Distinguishes the contracting EU entity from the ultimate parent — the distinction the whole transfer analysis rests on
- [x] Unresolved hosts surface as unresolved with the raw host shown, never silently dropped
- [x] Registry entries carry a review date; stale entries warn in CI

## How this is verified
```bash
pnpm test:unit -- vendor-resolve
```

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-04 10:25 — The vendor registry (packages/scanner/data/vendors/registry.json, VendorRegistrySchema in contracts): 17 entries, each with the entity a customer in the EEA contracts with and the ultimate parent, both with a country, a role, links by id to the DNS service map and the recipient host map (no second copy of any suffix), the terms URL and date it was read, and a reviewBy date. Resolver in packages/scanner/src/vendors: resolveHost picks the longest matching suffix across the entry's own suffixes and the suffixes of the maps it claims, returns resolved, unresolved with the host, or ambiguous with every candidate; resolveHosts keeps every host in order. entityFields turns an entry into the vendor row's legalEntity, jurisdiction (contracting), parentJurisdiction (when it differs) and transfer.outsideEea (either entity outside the EEA; mechanism unknown, never assumed). candidateProcessors now resolves each DNS service through the registry and records both map versions; a service the registry does not cover stays unresolved with the map's word. staleEntries names entries past reviewBy or read more than 180 days ago; auditRegistry checks every link resolves and nothing is claimed twice, and lists map entries with no legal entity yet. scripts/check-registries.ts (pnpm check:registries, in CI) fails on a broken link or a double claim and prints GitHub warnings for stale entries and uncovered map entries (today: zoho, atlassian, docusign, jsdelivr, onetrust, and one more, all left unclaimed rather than guessed). tests/unit/scanner/vendor-resolve.test.ts covers the registry, the distinction, the audit, staleness, host resolution through both maps, the unresolved path and the DNS rows.
- 2026-09-04 06:54 — claimed by claude
- 2026-09-04 07:02 — done — vendor registry with contracting entity and parent, provenance and review dates; resolver keeps unresolved hosts; check:registries warns in CI
