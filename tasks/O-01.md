---
id: O-01
title: Our own compliance, published
stream: O
phase: 6
est: M
status: done
owner: claude
claimed: 2026-09-04 11:29
completed: 2026-09-04 11:39
depends: G-01
verify: pnpm test:integration -- dogfood
---
# O-01 · Our own compliance, published

## Goal
Run the product against ourselves and publish the result — case, register, supply chain, processing agreement, sub-processor list.

## Acceptance criteria
- [x] Our own case is public and current
- [x] Sub-processor list, retention schedule and deletion path exist before the first paying customer
- [x] A test fails if our published sub-processor list drifts from the services actually configured

## How this is verified
```bash
pnpm test:integration -- dogfood
```

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-04 19:20 — packages/artefacts/content/ourselves.json and src/ourselves.ts: our own record, in en/da/de: the controller (the operator named in the footer), what we process and on what basis (scan evidence, answers and invitations, claims and the timeline), who processes it for us by purpose (hosting, the database, the model, mail) with what each receives and the contractual basis, the public sources we read (every corpus, registry and store host in endpoints.json with who runs it and where), the retention schedule rendered from the database's own RETENTION map in words, and the deletion path in four steps. auditOurselves is the drift guard: every declared source host published with the right purpose and country, nothing published that is not declared, every configured service (model, database from the running config) with a processor entry whose host matches once contracted, and no pending processor in production. The deployment-bound processors are pending until contracted; the record says so on the page rather than naming a provider that does not exist yet. apps/web: /[locale]/ourselves renders the record from the declared endpoints and RETENTION at request time (force-dynamic) with a Markdown twin at /[locale]/ourselves.md, a footer link on every page, and the sitemap lists it; our own case's public progress page is linked when OURSELVES_TRUST_SLUG is set, and the page says so until then. @gc/config gains declaredEndpoints(). tests/integration/dogfood.test.ts: the audit passes against endpoints.json and the test configuration and fails on every kind of drift (a new source, a removed one, a moved jurisdiction, a contracted processor at another host, a contracted processor without an entity, a pending one in production); the retention schedule covers every table in every language; the deletion route, function, event and steps exist; the document renders in three languages without fallback and drops a source the moment it leaves the declaration.
- 2026-09-04 11:29 — claimed by claude
- 2026-09-04 11:39 — done — our own record published from the running configuration: processing, processors, public sources, retention schedule, deletion path; a test fails on drift from endpoints.json and the config
