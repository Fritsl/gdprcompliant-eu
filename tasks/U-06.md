---
id: U-06
title: Guide pages
stream: U
phase: 2
est: M
status: done
owner: claude
claimed: 2026-09-04 06:37
completed: 2026-09-04 06:54
depends: U-01, R-03
verify: pnpm test:e2e -- guides
---
# U-06 · Guide pages

## Goal
One page per finding type per locale, generated from the remedy content, built to be found.

## Acceptance criteria
- [x] Static generation with correct metadata, canonical URLs and hreflang across locales
- [x] Each page ends in a scan entry point
- [x] Content comes from the remedy catalogue; there is no second copy of the text

## How this is verified
```bash
pnpm test:e2e -- guides
```

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-04 09:50 — The guide pages are generated at build time: generateStaticParams returns every (locale, id) pair the guide content is written in (guideLocales in @gc/remedies: en and da for all 26), dynamicParams is off, and a guide is never served in a language it is not written in (404), so /de/guides/<id> does not exist while /de/guides lists each guide in the language it exists in with hreflang and lang on the link. generateMetadata gives each page its title, description (the guide's 'wrong'), keywords, canonical address, the hreflang set with x-default, and Open Graph, all from the one origin helper (apps/web/lib/site.ts, APP_BASE_URL, also used by the case links). apps/web/app/sitemap.ts lists the front doors, the guide indexes and every guide with alternates; robots.ts keeps token pages, scan, demand and the deletion receipt out. Every guide page and the index end in the scan form, the same ScanForm component the front door renders. Found on the way: in Next 16 a child segment's generateStaticParams gets the parent's params as a promise, and returning only the child's segment generated nothing; returning the full pair from the bottom up does. tests/e2e/guides.test.ts builds with the test origin when the sample page is missing or built for another, then checks the static files on disk, the head, the text against the content, the form, the index, the sitemap and robots.
- 2026-09-04 06:37 — claimed by claude
- 2026-09-04 06:54 — done — guide pages static per locale with canonical, hreflang, sitemap, robots and the scan form at the end; e2e guides suite
