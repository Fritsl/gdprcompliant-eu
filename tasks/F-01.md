---
id: F-01
title: Monorepo scaffold and toolchain
stream: F
phase: 1
est: M
status: todo
depends: X-10
verify: pnpm install && pnpm -r typecheck && pnpm test:unit
---
# F-01 · Monorepo scaffold and toolchain

## Goal
pnpm workspace, TypeScript strict, ESLint, Prettier, Vitest, and a CI workflow that runs on every push.

## Acceptance criteria
- [ ] pnpm install works from a clean clone on Linux and Windows
- [ ] tsconfig uses strict:true, noUncheckedIndexedAccess:true, and project references across packages
- [ ] A deliberately failing test fails CI; removing it makes CI green
- [ ] Layout matches PLAN.md §Repo layout exactly — later tasks assume those paths

## How this is verified
```bash
pnpm install && pnpm -r typecheck && pnpm test:unit
```

## Notes
No app code in this task. The only deliverable is that the harness runs.

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
