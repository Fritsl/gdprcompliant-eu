---
id: T-01
title: The fixture site suite
stream: T
phase: 2
est: L
status: done
owner: claude
claimed: 2026-09-04 04:18
completed: 2026-09-04 04:46
depends: F-07
verify: pnpm test:integration -- fixture-suite
---
# T-01 · The fixture site suite

## Goal
Twenty-five deliberately broken websites, each isolating known violations, plus clean controls.

## Acceptance criteria
- [x] Every launch finding type has at least one positive and one negative fixture
- [x] At least four fixtures are clean sites that must produce zero findings
- [x] Fixtures cover the awkward cases: lazy-loaded trackers, banners in shadow DOM, iframes, single-page apps, consent stored in localStorage rather than cookies
- [x] Adding a fixture requires no code change

## How this is verified
```bash
pnpm test:integration -- fixture-suite
```

## Notes
This is the substitute for a human tester. Its coverage is the ceiling on how much anyone can trust the scanner.

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-04 10:30 — twenty-six fixtures. tests/integration/fixture-suite.test.ts scans every non-hostile fixture the way a case is scanned (all five page families, the scanner's own dwell, assembled against the DK catalogue) and holds it to its expected.json: every must raised, no mustNot raised, and a `clean` control raising nothing at all; across the estate every finding type the scanner raises from a page has a positive and a negative fixture (the certificate-log family is the one exception, proved by its own cassettes), the awkward-case tags are each present, and a fixture is a directory (the unit test stopped naming them). Four clean controls: clean-shop-gated (a banner that gates its tracker, one click to refuse, choice remembered), clean-blog (no scripts, nothing stored), clean-agency (a contact form over TLS with the notice beside it) and clean-spa (hash routes, consent in localStorage, tracker gated). Making them clean surfaced three scanner gaps, fixed: policy discovery now follows hash routes (a single-page app's policy is a route and a same-document navigation), the replay detector now looks at the pages the form inventory visited (REC-01 on the intake page in a real scan, not only in its own test), and the form-page hint knows intake, patient, booking and appointment pages. The brochure gained the policy a clean site has; banner-two-layer declares the three path findings it raises; insecure-forms declares POL-01; the cloaked shop answers to its own tracker host so no two fixtures share a name.
- 2026-09-04 04:18 — claimed by claude
- 2026-09-04 04:46 — done — fixture suite: 26 fixtures scanned as cases and held to expected.json, four clean controls, coverage per finding type; hash-route policies, replay on form pages
