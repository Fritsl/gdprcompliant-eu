---
id: G-02
title: Privacy policy and cookie declaration generators
stream: G
phase: 5
est: L
status: done
owner: claude
claimed: 2026-09-04 07:44
completed: 2026-09-04 07:57
depends: G-01, A-09
verify: pnpm test:evals -- policy-generation
---
# G-02 · Privacy policy and cookie declaration generators

## Goal
Documents generated from the register, so they describe what is actually happening.

## Acceptance criteria
- [x] Every statement in the output traces to a graph row; nothing is boilerplate filler
- [x] Refuses to generate where the register has gaps, naming the gaps instead of inventing text
- [x] Output in the target locale, legally structured for the target jurisdiction
- [x] Generated documents are re-checked against the live site by G-05

## How this is verified
```bash
pnpm test:evals -- policy-generation
```

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-04 13:40 — Deterministic generators, no model: privacyPolicyDocument and cookieDeclarationDocument in @gc/artefacts, templates in packages/artefacts/content/policy.json (en, da, de). The policy walks the twelve Article 13 elements in the order the clause analysis (S-10) labels them, so G-05 can compare a generated policy with a scanned one like for like; every paragraph is a template filled from a confirmed register row, an answer or the jurisdiction's authority, and carries the ids it came from in a trace comment (tracesOf / withoutTraces); the preview page hides the traces, the export keeps them. policyGaps and cookieGaps name what is missing (no confirmed activity, a row still a draft, a row without a basis or a retention answer, no controller contact; no cookies read, a cookie the database does not know) and the generator refuses while any remain. In @gc/db: recordAnswer (questions controller.address, controller.email, controller.dpo), draftDocument, documentGaps, generateDocument (stores a draft version through generateArtefact, the sign-off gate untouched). The cookie declaration classifies the first load's cookie evidence rows against the cookie database and lists them by category with provider, purpose and expiry. The web preview page lists the gaps or offers the button; POST artefacts/[kind]/generate writes the draft. Register vocabulary got German. tests/evals/policy-generation.test.ts: refusal names every gap; every statement traces to a graph row or an answer and the markdown carries the same traces; Danish for a DK case with Datatilsynet, German for a DE case with the German authority, same structure; the cookie declaration refuses on an unknown cookie and lists the rest. The evals project now runs in CI with the database. G-05 (waiting) will run the re-check the traces and section ids exist for.
- 2026-09-04 07:44 — claimed by claude
- 2026-09-04 07:57 — done — policy and cookie declaration written from the graph, every paragraph traced, refusal names the gaps, da/de with the jurisdiction's authority; evals in CI
