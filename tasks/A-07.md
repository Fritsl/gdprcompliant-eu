---
id: A-07
title: The verifier gate
stream: A
phase: 2
est: L
status: done
owner: claude
claimed: 2026-09-03 23:34
completed: 2026-09-03 23:41
depends: F-04, A-08
verify: pnpm test:evals -- verifier && pnpm test:adversarial -- claim-poisoning
---
# A-07 · The verifier gate

## Goal
Nothing enters the graph without an evidence pointer and, for legal claims, a citation that actually resolves.

## Acceptance criteria
- [x] Quoted evidence is confirmed by exact substring match against the stored source, in code, not by a model
- [x] Citations are resolved against the corpus for the stated jurisdiction; an unresolvable citation rejects the claim
- [x] A second-pass model check runs on claims that pass the mechanical checks
- [x] Rejections are recorded with a reason and are visible in an internal review queue
- [x] Poisoned-input suite: 50 deliberately false or fabricated claims, ≥ 98% rejected

## How this is verified
```bash
pnpm test:evals -- verifier && pnpm test:adversarial -- claim-poisoning
```

## Notes
Scheduled in phase 2 despite being agent work, because S-10 needs it. It is the single most load-bearing safety component.

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-04 02:50 — verifyClaim in @gc/agent: evidence exists with the pointer's hash in the claim's case; quotes by exact substring of the stored body; every citation resolves for the claim jurisdiction at the claim corpus version, with law quotes checked against the passage as published; the review_claim second pass runs only after all of that, reads evidence fenced as untrusted, and can reject but never accept (an unavailable model rejects). createVerifier in @gc/corpus wires evidence (tenant), corpus_chunks and the decisions registry, writes claim_verdicts (migration 0010, RLS, with_case, delete_case) and a claim_rejected timeline event; reviewQueue() is a SECURITY DEFINER read of unreviewed rejections across tenants, markReviewed() clears one. Poisoned suite: 50 claims, 50 rejected with the model stubbed to say yes; 10 true claims accepted. The eval's model stage runs only with MODEL_BASE_URL set and says so otherwise. No operator page for the queue yet; it is a function and a table until there is an operator login.
- 2026-09-03 23:34 — claimed by claude
- 2026-09-03 23:41 — done — verifyClaim: mechanical checks in code, model second pass that can only reject, verdicts recorded with a review queue; 50/50 poisoned claims rejected
