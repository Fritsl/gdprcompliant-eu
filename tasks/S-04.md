---
id: S-04
title: Pass B and Pass C collectors
stream: S
phase: 2
est: M
status: done
owner: claude
claimed: 2026-09-04 00:42
completed: 2026-09-04 00:53
depends: S-03
verify: pnpm test:integration -- pass-bc
---
# S-04 · Pass B and Pass C collectors

## Goal
Reject-everything and accept-everything passes producing the same shape of capture as Pass A.

## Acceptance criteria
- [x] Pass B asserts that a refusal was actually registered, not merely clicked
- [x] Pass C records the full permitted vendor set — this is the inventory later phases consume
- [x] A stated choice is re-checked after reload; a banner that forgets is a finding
- [x] All three passes run concurrently where safe and complete inside the performance budget

## How this is verified
```bash
pnpm test:integration -- pass-bc
```

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-04 08:10 — The capture machinery moved out of Pass A into passes/instrument.ts (requests with initiator chains, cookies, storage, document info, phase marks); Pass A is unchanged in behaviour. collectPassB loads, refuses on the page (refuseOnPage), records what was written to remember the choice (new cookies and storage keys), reloads, and captures the reload alone; the refusal counts as registered only when the banner stays away on the reload, otherwise the capture carries the CNS-04 finding (new detector, remedy cns-04-remember-the-choice, bindings DK/DE). collectPassC does the same with the accept control (platform selectors or the yes-to-all text in da/en/de) and hands over vendorHosts, the third-party inventory. collectPasses runs A, B and C on their own contexts at once; on the reject-not-honoured fixture the three take about 2.4 s together. PassCapture gained an optional consent block. Fixture banner-forgets (refuses, stores nothing, asks again on reload) added; every banner fixture now stores an acceptance as a real platform would. Not here: the B/C network diff that becomes CNS-02 (S-05).
- 2026-09-04 00:42 — claimed by claude
- 2026-09-04 00:53 — done — Pass B refuses and checks the refusal registered on reload, Pass C accepts and hands over the vendor set, a banner that forgets is CNS-04, all three passes concurrent
