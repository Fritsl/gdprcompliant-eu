---
id: T-12
title: Delivery gate script and manual smoke checklist
stream: T
phase: 6
est: M
status: done
owner: claude
claimed: 2026-09-04 16:15
completed: 2026-09-04 16:19
depends: T-06, T-09, T-10
verify: pnpm run gate -- --dry-run
---
# T-12 · Delivery gate script and manual smoke checklist

## Goal
One command, one verdict, plus the short list of things a human should still look at with their own eyes.

## Acceptance criteria
- [x] Aggregates every suite plus canary status into a single pass or fail
- [x] Manual checklist is under fifteen items and each is a yes-or-no observation, not a judgement call
- [x] Produces the handover report referenced in O-05

## How this is verified
```bash
pnpm run gate -- --dry-run
```

## Notes
The manual list is deliberately tiny. Everything else has to be automated, because the user cannot test this by hand.

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-04 22:00 — scripts/gate.ts, run as `pnpm run gate`: sixteen invariants (types, lint, formatting, RLS, retention, finding completeness, citations, guide snippets, claims, registries, rule citations, i18n coverage, the schema, findings, remedy-lock and eval-history documents), then every suite the manifest in tests/suites.ts marks required, each under its own wall-clock budget, then the canary comparison; invariants first so a wrong file stops the gate before a browser opens. Any red stops the run and is named with the task it proves and that task's owner, read from the task file; a required suite whose tools are absent (no database, no model, no browser, no cassettes) is a red, not a pass, because the gate cannot vouch for what it did not run; a nightly suite informs and never blocks. `--dry-run` prints the plan and the environment and writes the report without executing anything; `--only <step>` runs one. The report lands in artifacts/gate/ as markdown and JSON, dated, with the commit, the environment, every step's result and time against budget, the reds with their tails, the manual checklist appended for ticking, and a seal: a sha256 over the report's own content plus the name of who ran it, verifiable with verifySignature(). docs/smoke-checklist.md holds the fourteen manual items, each a yes-or-no observation a person makes with their own eyes (print preview, Danish and German, a phone-width window, the trust page's look, the PDF's disclaimer, the map, the advisor's notice and a refusal, the timeline, a deletion); tests/unit/gate.test.ts holds the list under fifteen, numbered, questions, without judgement words, and holds the plan to the manifest, every step to an existing task, the red-stops rule, the missing-tools rule and the seal. O-05 wires it into README and CI.
- 2026-09-04 16:15 — claimed by claude
- 2026-09-04 16:19 — done — pnpm run gate: invariants, required suites under budget, canary; a red stops and names task and owner; dated, sealed report with the 14-item manual checklist; --dry-run prints the plan
