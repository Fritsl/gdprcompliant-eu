---
id: R-02
title: Enforce: no finding without a remedy
stream: R
phase: 2
est: S
status: done
owner: claude
claimed: 2026-09-03 20:15
completed: 2026-09-03 20:19
depends: R-01, F-03
verify: pnpm run check:finding-completeness
---
# R-02 · Enforce: no finding without a remedy

## Goal
Make the product rule a structural impossibility rather than a guideline — database constraint, type-level requirement and a CI check.

## Acceptance criteria
- [x] NOT NULL foreign key from findings to remedies in the database
- [x] The Finding type cannot be constructed without a remedy reference
- [x] CI check enumerates every registered detector and fails if any lacks a remedy in every supported jurisdiction
- [x] The no_solution kind satisfies the constraint and writes to the demand ledger

## How this is verified
```bash
pnpm run check:finding-completeness
```

## Notes
Small task, load-bearing. This is the sentence from the concept doc turned into a constraint.

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-03 20:15 — claimed by claude
- 2026-09-03 claude: three layers, one check. Database: findings.remedy_id + remedy_version NOT NULL with a composite FK to remedies (F-03 migration; the check greps the committed SQL). Type: FindingSchema requires remedy (F-04; the check parses a finding without one and expects refusal). Catalogue: packages/findings/content/detectors.json registers what the code can raise (SEC-01..07, POL-01); checkFindingCompleteness unions that with every fixture's findings.must and demands a remedy in every SUPPORTED_JURISDICTION — scripts/finding-completeness.ts (pnpm run check:finding-completeness) fails CI on a gap and names who promised the finding. Added cns-01-gate-before-interaction because the lazy-tracker fixture promised CNS-01. resolveAndRecord() writes a no_solution outcome to a DemandLedger (interface + MemoryDemandLedger; the table is R-05).
- 2026-09-03 20:19 — done — Detector registry, completeness check across fixtures and jurisdictions, no_solution writes to the demand ledger; DB and type constraints verified by the same check
