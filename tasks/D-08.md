---
id: D-08
title: Supply-chain map
stream: D
phase: 4
est: M
status: done
owner: claude
claimed: 2026-09-04 13:59
completed: 2026-09-04 14:17
depends: D-07
verify: pnpm test:e2e -- supply-chain-map
---
# D-08 · Supply-chain map

## Goal
The diagram the customer has never seen, exportable and shareable.

## Acceptance criteria
- [x] Renders legibly at 3 levels and 60 nodes without becoming spaghetti
- [x] Nodes carry jurisdiction and the evidence link that placed them there
- [x] Exports to PNG and PDF; readable in greyscale

## How this is verified
```bash
pnpm test:e2e -- supply-chain-map
```

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-05 08:10 — One model, three drawings. layoutSupplyChain (packages/artefacts/src/supply-chain-map.ts) takes the company, the processors the register names (drafts included, since a supplier read from the site belongs on the map) and the chain the walk wrote (D-07), and lays them out in columns by level, rows by name, the shorter columns centred, under a hard cap of sixty nodes and three levels beyond the company; what the cap leaves off is counted and written in the legend. No two boxes touch (overlappingNodes, checked in unit and in the browser by bounding boxes); labels are cut to fit and the browser test measures every text run against the box. Every node carries its country and, where the record has it, the id of the evidence that placed it and a link to it: the page lists those evidence rows with caption, date and source under the map, and each box links to its row. The SVG uses greys only, level told by shape (double border, plain box, rounded dashed, rounded dotted) and a cycle by a dashed edge, so it reads the same in greyscale; a unit test refuses any non-grey colour in the SVG and any unequal colour operator in the PDF. The PDF is landscape A4 from the same model with pdfkit; the PNG is the SVG rasterised by sharp at 144 dpi on white in the web route. Routes: /c/[token]/supply-chain (page), .svg, .png, .pdf, all from the graph as it stands; the case page links the map. tests/e2e/supply-chain-map.test.ts seeds sixty-one companies over three levels, opens the page, checks the sixty boxes, their jurisdictions and evidence links against the listed rows, measures the labels, fetches the three exports (PNG signature and size, PDF header) and follows the link from the case page in Danish. Unit: the layout at the cap, the cycle, the greys, the shapes, three languages, the PDF metadata.
- 2026-09-04 13:59 — claimed by claude
- 2026-09-04 14:17 — done — One model drawn as SVG, PNG and PDF in greys only; sixty nodes over three levels without a box touching another; every node with its jurisdiction and a link to the evidence that placed it
