---
id: F-09
title: Network record and replay
stream: F
phase: 1
est: M
status: todo
depends: F-07
verify: GC_NETWORK=replay pnpm test:integration
---
# F-09 · Network record and replay

## Goal
Every outbound HTTP, DNS and registry call goes through a recorder; CI replays cassettes and blocks live egress entirely.

## Acceptance criteria
- [ ] A CI run with the network cable pulled still passes the whole suite
- [ ] Cassettes are committed, human-readable, and redacted of anything resembling a credential
- [ ] A missing cassette in CI is a hard error, never a silent live call
- [ ] Re-recording is one documented command

## How this is verified
```bash
GC_NETWORK=replay pnpm test:integration
```

## Notes
Without this the suite is flaky and nobody trusts it, which defeats the whole point of the user not testing manually.

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
