---
id: L-03
title: Commercial queue
stream: L
phase: 5
est: S
status: done
owner: claude
claimed: 2026-09-04 10:05
completed: 2026-09-04 10:07
depends: L-01
verify: pnpm test:unit -- commercial-queue
---
# L-03 · Commercial queue

## Goal
Open cases ranked by qualification, severity and how much of it we can actually solve.

## Acceptance criteria
- [x] Ranking is explainable per row
- [x] Contact happens about a finding, never about tool usage — the queue surfaces the finding to open with

## How this is verified
```bash
pnpm test:unit -- commercial-queue
```

## Log
<!-- Append one line per meaningful event. Do not rewrite history. -->
- 2026-09-04 12:55 — packages/db/src/queue.ts. rankCase is pure: a row's rank is the lane score × the weight of the worst open finding (blocking 3, serious 2, advisory 1) × the share of open findings whose remedy we can act on (self_fix, generated_artefact, our_product, partner_alternative; no_solution is a gap), rounded; `why` is three lines a person can read (signal and lane; how many open and the worst; how many of them we can solve). The hook is the worst open finding we can do something about, ties by type id, so every call opens with a finding; a case with nothing open, or only gaps, has no hook and ranks zero. rankQueue sorts by rank, then open count, then case number, so the order is stable. loadQueue reads every live case across tenants as the owner (cases and findings only; the module reads no event, visit, login or export), maps remedy ids to kinds through the caller's catalogue, and ranks. tests/unit/db/commercial-queue.test.ts: the formula and the three lines, the hook choice, the empty and gaps-only cases, the ranking of four cases from the prototype's queue, stability, and that no row and no loader source mentions usage.
- 2026-09-04 10:05 — claimed by claude
- 2026-09-04 10:07 — done — signal × severity × what we can solve; three lines of why per row; every row opens with a finding
