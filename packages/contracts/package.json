{
  "name": "@gc/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --build",
    "test": "pnpm -w exec vitest run --project unit tests/unit/contracts"
  },
  "dependencies": {
    "zod": "^4.5.4"
  }
}
