// The lock and the evidence store hash the same way. The definition lives in contracts;
// scripts/remedy-lock.mjs carries a plain-JavaScript copy that the lock test proves equal.
export { canonicalJson, sha256 } from '@gc/contracts';
