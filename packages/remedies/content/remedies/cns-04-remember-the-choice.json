{
  "id": "cns-04-remember-the-choice",
  "version": 1,
  "findingTypeId": "CNS-04",
  "jurisdictions": "all",
  "kind": "self_fix",
  "title": {
    "en": "Make the banner remember the answer",
    "da": "Få banneret til at huske svaret"
  },
  "effort": {
    "label": {
      "en": "About 15 minutes",
      "da": "Cirka 15 minutter"
    },
    "minutes": 15
  },
  "detail": {
    "en": "The visitor said no, the banner closed, and on the next page it asked again. A choice that is not stored is not a choice: store it, read it on every load, and show the banner only when there is none.",
    "da": "Den besøgende sagde nej, banneret lukkede, og på næste side spurgte det igen. Et valg, der ikke gemmes, er ikke et valg: gem det, læs det ved hver indlæsning, og vis kun banneret, når der ikke er et."
  },
  "snippet": "const KEY = 'consent';\nconst stored = localStorage.getItem(KEY);\nif (!stored) showBanner();\n\nfunction decide(categories) {\n  localStorage.setItem(KEY, JSON.stringify({ categories, at: Date.now() }));\n  document.cookie = `${KEY}=${encodeURIComponent(categories.join(','))}; path=/; max-age=15552000`;\n  hideBanner();\n}",
  "verifyLabel": {
    "en": "Re-check this finding",
    "da": "Tjek dette fund igen"
  },
  "verification": {
    "method": "rescan"
  },
  "action": {
    "kind": "agent_prompt",
    "label": {
      "en": "Paste into your coding assistant"
    },
    "forwardable": {
      "en": "Or forward it to your developer — it reads the same either way."
    },
    "body": {
      "en": "On {{domain}} the cookie banner closes when a visitor refuses, but it comes back on the next page load: the refusal is not stored anywhere.\n\nStore the visitor's choice (a cookie or local storage entry with the chosen categories and a timestamp) when they decide, read it before showing the banner on every page, and show the banner only when there is no stored choice. Treat a refusal exactly like an acceptance for this purpose: it is remembered for the same length of time.\n\nWhen you are done: tell me where the choice is stored and how to verify that a refusal survives a reload."
    }
  }
}
