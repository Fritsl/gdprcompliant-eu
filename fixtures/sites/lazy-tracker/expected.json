{
  "site": "lazyshop.test",
  "description": "A shop with no banner at all whose tracker arrives four seconds after the page loads, on a timer. A half-second network-idle wait never sees it.",
  "tags": ["lazy-load"],
  "findings": {
    "must": ["CNS-01"],
    "mustNot": ["FRM-02", "SEC-02"]
  },
  "network": {
    "firstLoad": {
      "mustContact": ["lazy.tracker.test"],
      "mustNotContact": []
    }
  }
}
