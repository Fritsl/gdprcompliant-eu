{
  "name": "@gc/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --build",
    "start": "node --env-file-if-exists=../../.env --import tsx src/main.ts"
  },
  "dependencies": {
    "@gc/config": "workspace:*",
    "@gc/contracts": "workspace:*",
    "@gc/db": "workspace:*",
    "@gc/findings": "workspace:*",
    "@gc/jobs": "workspace:*",
    "@gc/remedies": "workspace:*",
    "@gc/scanner": "workspace:*",
    "zod": "^4.5.4",
    "@gc/telemetry": "workspace:*"
  }
}
